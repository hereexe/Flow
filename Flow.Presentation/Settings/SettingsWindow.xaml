<Window x:Class="Flow.Presentation.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:domain="clr-namespace:Flow.Domain;assembly=Flow.Domain"
        Title="Flow Settings"
        Width="440" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        FontFamily="Segoe UI, Inter"
        Background="Transparent"
        AllowsTransparency="True"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    
    <Window.Resources>
        <!-- Toggle Switch (Whisper Pill Style: 36x20 track, 14x14 thumb, 10px radius) -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextMainBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <Border x:Name="Track" Grid.Column="1" Width="36" Height="20" CornerRadius="10" 
                                    Background="{DynamicResource ToggleTrackBrush}" 
                                    VerticalAlignment="Center">
                                <Ellipse x:Name="Thumb" Width="14" Height="14" 
                                         Fill="{DynamicResource ToggleThumbBrush}" 
                                         HorizontalAlignment="Left" Margin="3,0,3,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform X="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                <Setter TargetName="Thumb" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="16"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Label -->
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="FontSize" Value="10.5"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        
        <!-- Muted Hint Text -->
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="FontSize" Value="9.5"/>
        </Style>

        <!-- TextBox Style (Whisper Pill: 12px CornerRadius, 32px unified height) -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMainBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12"
                                Height="{TemplateBinding Height}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMainBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}" Margin="2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboBoxItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboBoxItemSelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style (Whisper Pill: 12px CornerRadius, 32px unified height) -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMainBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid Height="{TemplateBinding Height}">
                            <ToggleButton Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          Focusable="false" 
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" 
                                                      Stroke="{DynamicResource TextMutedBrush}" 
                                                      StrokeThickness="1.5" 
                                                      VerticalAlignment="Center" 
                                                      HorizontalAlignment="Center" 
                                                      Margin="0,0,12,0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                                              VerticalAlignment="Center" 
                                              HorizontalAlignment="Left"
                                              Margin="12,0,28,0"
                                              IsHitTestVisible="False"/>

                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{DynamicResource PopupBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}" Margin="0,4,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="16" Color="Black" Opacity="0.3" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style (Whisper Pill: 14px CornerRadius) -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <!-- Outer Whisper Pill Window Container (22px radius, clean per-pixel alpha) -->
        <Border Background="{DynamicResource WindowBackgroundBrush}" 
                CornerRadius="{DynamicResource WindowCornerRadius}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" 
                                  Color="Black" 
                                  Opacity="0.4" 
                                  ShadowDepth="6" 
                                  Direction="270"/>
            </Border.Effect>

            <!-- Inner Top Inset Highlight -->
            <Border CornerRadius="21"
                    BorderThickness="0,1,0,0"
                    BorderBrush="{DynamicResource InsetHighlightBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Window Header -->
                    <Border Grid.Row="0" 
                            BorderBrush="{DynamicResource HeaderBorderBrush}" 
                            BorderThickness="0,0,0,1" 
                            Padding="18,12" 
                            MouseLeftButtonDown="Header_MouseLeftButtonDown" 
                            Background="Transparent">
                        <Grid>
                            <TextBlock Text="Flow Settings" 
                                       FontSize="13" 
                                       FontWeight="Medium" 
                                       Foreground="{DynamicResource TextMainBrush}" 
                                       VerticalAlignment="Center"/>
                            
                            <!-- Circular Window Control Buttons (24x24 px, 12px radius) -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Content="&#xE921;" 
                                        FontFamily="Segoe MDL2 Assets" 
                                        FontSize="9" 
                                        Width="24" 
                                        Height="24" 
                                        Margin="0,0,4,0"
                                        Background="Transparent" 
                                        BorderThickness="0" 
                                        Foreground="{DynamicResource WindowControlBrush}"
                                        Cursor="Hand" 
                                        Click="MinimizeButton_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1AFFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                
                                <Button Content="&#xE8BB;" 
                                        FontFamily="Segoe MDL2 Assets" 
                                        FontSize="9" 
                                        Width="24" 
                                        Height="24" 
                                        Background="Transparent" 
                                        BorderThickness="0" 
                                        Foreground="{DynamicResource WindowControlBrush}"
                                        Cursor="Hand" 
                                        Click="CloseButton_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E81123"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- Window Body -->
                    <Border Grid.Row="1" Padding="18,14,18,18">
                        <StackPanel>
                            <!-- Row 1: App Theme & Translation Hotkey -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="App Theme" Style="{StaticResource SectionLabel}"/>
                                    <ComboBox ItemsSource="{Binding AvailableThemes}" SelectedItem="{Binding SelectedTheme}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Translation Hotkey" Style="{StaticResource SectionLabel}"/>
                                    <TextBox x:Name="HotkeyTextBox"
                                             Text="{Binding Hotkey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             IsReadOnly="True"
                                             IsReadOnlyCaretVisible="True"
                                             PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                             GotFocus="HotkeyTextBox_GotFocus"
                                             LostFocus="HotkeyTextBox_LostFocus"
                                             ToolTip="Click and press a key combination to set the hotkey"/>
                                </StackPanel>
                            </Grid>

                            <!-- Row 2: Source & Target Language -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Source Language" Style="{StaticResource SectionLabel}"/>
                                    <ComboBox ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding PrimaryLanguage}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Target Language" Style="{StaticResource SectionLabel}"/>
                                    <ComboBox ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding SecondaryLanguage}"/>
                                </StackPanel>
                            </Grid>

                            <!-- Row 3: Translation Provider -->
                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="Translation Provider (BYOK)" Style="{StaticResource SectionLabel}"/>
                                <ComboBox ItemsSource="{Binding AvailableProviders}" SelectedItem="{Binding ActiveOnlineProvider}"/>
                            </StackPanel>

                            <!-- Row 4: API Key -->
                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="API Key" Style="{StaticResource SectionLabel}"/>
                                <Grid Height="32">
                                    <PasswordBox x:Name="ApiPasswordBox" 
                                                 ToolTip="Enter the API key for the selected online provider"
                                                 Padding="12,0,32,0"
                                                 PasswordChar="•"
                                                 Height="32"
                                                 VerticalContentAlignment="Center"
                                                 PasswordChanged="ApiPasswordBox_PasswordChanged">
                                        <PasswordBox.Style>
                                            <Style TargetType="PasswordBox">
                                                <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource TextMainBrush}"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="FontSize" Value="12"/>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="PasswordBox">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                                    CornerRadius="12"
                                                                    Height="{TemplateBinding Height}">
                                                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsApiKeyVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </PasswordBox.Style>
                                    </PasswordBox>

                                    <TextBox x:Name="ApiTextBox" 
                                             Text="{Binding ApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             ToolTip="Enter the API key for the selected online provider"
                                             Padding="12,0,32,0"
                                             Height="32"
                                             VerticalContentAlignment="Center">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsApiKeyVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>

                                    <ToggleButton IsChecked="{Binding IsApiKeyVisible}" 
                                                  HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                  Margin="0,0,4,0" Width="24" Height="24" 
                                                  Background="Transparent" BorderThickness="0" Cursor="Hand">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <TextBlock Text="&#xE890;" FontFamily="Segoe MDL2 Assets" FontSize="11" 
                                                               Foreground="{DynamicResource TextMutedBrush}" 
                                                               HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="Icon"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Icon" Property="Opacity" Value="0.5"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                                <TextBlock Text="Stored in Windows Credential Manager" Style="{StaticResource MutedText}" Margin="2,3,0,0"/>
                            </StackPanel>

                            <!-- Thin Divider (Whisper Pill aw-sep) -->
                            <Border BorderBrush="{DynamicResource HeaderBorderBrush}" BorderThickness="0,1,0,0" Margin="0,2,0,8" Padding="0,8,0,0">
                                <CheckBox Content="Launch with Windows" 
                                          IsChecked="{Binding LaunchWithWindows}" 
                                          Style="{StaticResource ToggleSwitchStyle}"/>
                            </Border>

                            <CheckBox Content="Show HUD Indicator" 
                                      IsChecked="{Binding ShowHud}" 
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      Margin="0,0,0,16"/>

                            <TextBlock Text="{Binding ErrorMessage}"
                                       Foreground="#FFF87171"
                                       FontSize="11"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,10"
                                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>

                            <!-- Save Settings Button (14px CornerRadius) -->
                            <Button Command="{Binding SaveCommand}" IsDefault="True">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                        <Setter Property="Content" Value="Save Settings" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSaved}" Value="True">
                                                <Setter Property="Content" Value="Saved!" />
                                                <Setter Property="Background" Value="#34D399" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>
